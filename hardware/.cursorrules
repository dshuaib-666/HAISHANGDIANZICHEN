# Third项目 - STM32嵌入式系统开发规则

## 项目概述
这是一个STM32F4xx微控制器的嵌入式项目，主要功能包括：
- SD卡数据管理和存储
- ADC数据采集和处理
- 惯性导航传感器数据处理
- USB虚拟串口通信
- 备份SRAM数据管理

## 代码风格规范
- 使用C语言开发
- 变量命名采用驼峰命名法
- 函数名使用下划线分隔
- 常量定义使用全大写字母
- 结构体命名以Struct结尾

## 开发规则
1. **内存管理**
   - 谨慎使用动态内存分配
   - 优先使用静态缓冲区
   - 注意栈空间限制

2. **硬件相关**
   - 所有GPIO操作必须检查返回值
   - SD卡操作需要错误处理
   - ADC采样要考虑时序要求

3. **数据处理**
   - 使用备份SRAM存储关键配置
   - 数据包格式要保持64字节对齐
   - CRC校验必须正确实现

4. **通信协议**
   - 串口命令格式要统一
   - 错误信息要详细输出
   - 支持调试信息输出
5. **语法错误检查**
   “__attribute_”编译报错不是语法错误
6. **测试规则**
   - 不要调用系统命令进行测试，一般都会报错，由我来测试

## 文件结构
- `/User/` - 用户代码
- `/Core/` - STM32 HAL核心代码
- `/Drivers/` - 硬件驱动
- `/USB_DEVICE/` - USB设备代码
- `/Middlewares/` - 中间件

## 注意事项
- 始终检查HAL函数返回值
- 使用printf进行调试输出
- 保持代码注释为中文
- 遵循STM32 HAL库规范 